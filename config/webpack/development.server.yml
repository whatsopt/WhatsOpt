# Note: You must restart bin/webpack-dev-server for changes to take effect

default: &default
  enabled: true
  host: rdri206h.onecert.fr
  port: 8081

development:
  <<: *default

test:
  <<: *default
  enabled: false

production:
  <<: *default
  enabled: false
