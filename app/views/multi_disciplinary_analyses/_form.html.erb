<%= form_for(multi_disciplinary_analysis) do |f| %>
  <% if multi_disciplinary_analysis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multi_disciplinary_analysis.errors.count, "error") %> prohibited this multi_disciplinary_analysis from being saved:</h2>

      <ul>
      <% multi_disciplinary_analysis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
