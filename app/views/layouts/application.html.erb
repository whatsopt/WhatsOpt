<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>WhatsOpt</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <!-- Disable ActionCable <%= action_cable_meta_tag %> -->
  <%= csrf_meta_tags %>
  <meta name="relative-url-root" content="<%= Rails.application.config.action_controller.relative_url_root%>" />
  <%= favicon_link_tag asset_path('favicon-32.png'), :rel => 'icon', :type =>  'image/png' %>
</head>
<body>
	<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top" role="navigation">
	  <%= link_to mdas_path, class: "navbar-brand" do %> 
      <%= image_tag "logo_whatsopt_v1.0.svg", alt: "Logo", width: 32, height: 32, class: "d-inline-block align-top" %>
      <span>WhatsOpt</span> 
	  <% end %>
	  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
	    <span class="navbar-toggler-icon"></span>
	  </button>
	
    <% if user_signed_in? %>
	    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav mr-auto">
		    <li class="nav-item"><%= link_to "Analyses", mdas_path, class: "nav-link" %></li>
      <% if intranet? %>
        <li class="nav-item"><%= link_to "Notebooks", :notebooks, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "Hangar", geometry_models_path, class: "nav-link" %></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Redmine</a>
          <div  class="dropdown-menu" aria-labelledby="dropdown01">
            <%= link_to "Wiki", wiki_path, class: "dropdown-item" %>
            <%= link_to "Issues", issues_path, class: "dropdown-item" %>
          </div>
        </li>
      <% end %>
        </ul>
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="dropdown02" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.login %></a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown02">
                <%= link_to "My Profile", user_path(current_user), class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </div>
            </li>
        </ul>
	    </div>
    <% end %>	      
  </nav>

  <div class="container-fluid main-container">
    <%= render 'layouts/messages' %>
    <%= yield %>
  </div> 

  <div>
    <%= yield :document_ready %>
  </div>  
    
  <footer class="footer">
    <small>
      WhatsOpt, version:
      <% if user_signed_in? %>
        <%= link_to_unless_current version, changelog_path =%>, 
        <%= mail_to "remi.lafage@onera.fr", "contact", :encode => "hex"  %>
      <% else %>
        <%= version %>
      <% end %>
      </small>
  </footer>
</body>
</html>
