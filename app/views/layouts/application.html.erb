<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>WhatsOpt</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="relative-url-root" content="<%= Rails.application.config.action_controller.relative_url_root%>" />
</head>
<body>
	<nav class="navbar fixed-top navbar-toggleable-md navbar-inverse bg-inverse" role="navigation">
	<button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
		</button>
	<a class="navbar-brand" href="#">WhatsOpt</a>
	<div class="collapse navbar-collapse" id="navbarNavDropdown">
	<ul class="navbar-nav mr-auto">
		<!-- <li class="nav-item"><%= link_to "Projects", :projects, class: "nav-link" %></li> -->
		<li class="nav-item"><%= link_to "MDAs", mdas_path, class: "nav-link" %></li>
		<li class="nav-item"><%= link_to "Notebooks", :notebooks, class: "nav-link" %></li>
		<li class="nav-item"><%= link_to "OneraMDAO", oneramdao_path, class: "nav-link" %></li>
		<li class="nav-item"><%= link_to "JupyterHub", jupyterhub_path, class: "nav-link" %></li>
	</ul>
	<ul class="navbar-nav">
		<% if user_signed_in? %>
			<li class="nav-item dropdown">
			<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.login %></a>
			<div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
			<%= link_to "My Profile", user_path(current_user), class: "dropdown-item" %>
			<div class="dropdown-divider"></div>
			<%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
			</div>
			</li>
		<% else %>
			<li class="nav-item"><%= link_to "Log in", new_user_session_path, class: "nav-link" %></li>
		<% end %>
	</ul>
	</div>	      
    </nav>

    <div class="container main-container">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div> 

    <div>
      <%= yield :document_ready %>
    </div>  
    
    <footer class="footer">
    	<small>
	      WhatsOpt, version:
	      <% if user_signed_in? %>
	        <%= link_to_unless_current version, changelog_path =%>, 
		      <%= mail_to "remi.lafage@onera.fr", "contact", :encode => "hex"  %>
	      <% else %>
	        <%= version %>
	      <% end %>
      </small>
    </div>

</body>
</html>
